{
  "name": "add-projects-beta",
  "version": "1.0.0",
  "description": "add issues or PRs to GitHub Projects(Beta)",
  "main": "index.js",
  "repository": "git@github.com:sunadoi/add-projects-beta.git",
  "author": "sunadoi <y.sunadoi@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm index.js && esbuild --bundle --platform=node --allow-overwrite --outfile=index.js --target=node16 index.ts"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@octokit/graphql": "^4.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.23"
  }
}
