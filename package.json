{
  "name": "add-projects-beta",
  "version": "1.0.0",
  "description": "add issues or PRs to GitHub Projects(Beta)",
  "main": "index.js",
  "repository": "git@github.com:sunadoi/add-projects-beta.git",
  "author": "sunadoi <y.sunadoi@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm src/index.js && esbuild --bundle --platform=node --allow-overwrite --outfile=src/index.js --target=node16 src/index.ts"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@octokit/graphql": "^4.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.23"
  }
}
